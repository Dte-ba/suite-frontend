{{yield}}

{{#if (is-pending model.estado)}}
  <div class="ui active inline mini loader"></div>
{{else}}
  {{#ui-popup content=model.estado.nombre position='bottom left'}}
    {{estado-de-paquete model.estado.nombre}}
  {{/ui-popup}}
{{/if}}
