{{yield}}

{{#if (is-pending model.acompaniantes)}}
  <div class="ui active inline mini loader"></div>
{{else}}
  {{#each model.acompaniantes as |p|}}
    {{#suite-permiso permiso="personas.listar"}}
      {{#link-to 'app.personas.detalle' p}}{{p.nombreApellido}}{{/link-to}}
    {{else}}
      {{p.nombreApellido}}
    {{/suite-permiso}}
    {{#unless (eq p model.acompaniantes.lastObject)}}, {{/unless}}
  {{else}}
    Sin acompañantes
  {{/each}}
{{/if}}
