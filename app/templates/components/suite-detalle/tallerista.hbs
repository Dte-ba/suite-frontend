{{yield}}

{{#suite-promesa promesa=model.tallerista}}

  {{#suite-permiso permiso="personas.listar"}}
    {{#link-to "app.personas.detalle" model.tallerista}}
      {{model.tallerista.nombre}} {{model.tallerista.apellido}} ({{model.tallerista.region.nombre}})
    {{/link-to}}
  {{else}}
    {{model.tallerista.nombre}} {{model.tallerista.apellido}} ({{model.tallerista.region.nombre}})
  {{/suite-permiso}}

{{/suite-promesa}}
