{{yield}}

{{#unless disabled}}
  {{ui-calendar min=min type="date" date=fecha onChange=(action "cuandoCambiaFecha") placeholder="Seleccione fecha"}}
{{else}}
  <input class="ui disabled input" value={{fecha}}>
{{/unless}}
