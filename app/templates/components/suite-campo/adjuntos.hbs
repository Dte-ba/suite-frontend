<input
  multiple="true"
  onchange={{action "upload"}}
  accept="image/png,image/jpeg"
  type="file"/>

{{#if archivos}}
  <small>Actualmente:
  {{#each archivos as |a|}}
    {{a.name}}
    {{#unless (eq a archivos.lastObject)}}, {{/unless}}
  {{/each}}
</small>
{{/if}}

{{#if procesarImagenes.isRunning}}
  Cargando archivos ...
{{/if}}
