{{yield}}

{{#if tieneDatos}}
  {{#g-map lat=lat lng=lng zoom=16 as |context|}}
    {{g-map-marker context lat=lat lng=lng}}
  {{/g-map}}
{{else}}
  <div class="sin-mapa">
    Sin datos de ubicación
  </div>
{{/if}}
