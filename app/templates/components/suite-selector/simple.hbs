{{yield}}

{{#power-select
  options=opciones
  selected=valor
  disabled=deshabilitado
  searchEnabled=false
  onchange=cuandoSelecciona
  as |opcion|
}}
  {{get opcion atributoPrimario}}
{{/power-select}}
