{{yield}}

{{#if obtenerRegiones.last.running}}
{{else}}
  {{suite-selector/simple
    opciones=opciones
    deshabilitado=deshabilitado
    valor=valor
    searchEnabled=true
    cuandoSelecciona=cuandoSelecciona}}
{{/if}}
