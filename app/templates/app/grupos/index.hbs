{{outlet}}

<div class="ui secondary pointing fluid menu">
    <div class="menu">
      {{#link-to "app.grupos.index" class="ui item"}}Listado{{/link-to}}
    </div>
</div>

{{suite-tabla
  pagina=pagina
  columnas=model.columnas
  tarea=model.tareaGrupos
  paginacion=true
}}
