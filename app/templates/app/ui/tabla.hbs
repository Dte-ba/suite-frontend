{{outlet}}

<h1>Tablas</h1>

<p>Para incluir tablas se puede usar el
  el componente <code>suite-tabla-simple</code> incluyendo
  filas de la siguiente forma:
</p>

{{suite-tabla-simple filas=filas columnas=columnas}}

<h2>Versión con filtros</h2>

<p>También se puede incluir una tabla más completa usando
  el componente <code>suite-tabla-con-filtros</code> así:</p>

{{suite-tabla-con-filtros filas=data columnas=columns}}
