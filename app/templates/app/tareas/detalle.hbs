{{outlet}}
<div class="ui stackable grid">

  <div class="ui row middle aligned segment">
    <div class="ten wide column">
      <h3 class="ui header">
        {{model.titulo}} - {{model.id}} - {{model.fechaDeAlta}}
      </h3>
    </div>
    <div class="four wide column"></div>
    <div class="two wide column">
      <!-- {{#link-to "app.tareas.edicion" model class="ui button primario"}}Editar{{/link-to}} -->
    </div>
  </div>

  <div class="ui equal width row middle aligned segment">
    <div class="column">
      <i class="ui check circle icon"></i> {{model.prioridadDeTarea.nombre}}
    </div>
    <div class="column">
      <i class="ui server icon"></i> {{model.estadoDeTarea.nombre}}
    </div>
  </div>


    {{suite-detalle-escuelas
        numero="1"
        titulo="Características"
        model=model
        filas=model.filaCaracteristicas
    }}
    {{suite-detalle-escuelas
        numero="2"
        titulo="¿Dónde se realizará?"
        model=model
        filas=model.filaEscuela
    }}

</div>
