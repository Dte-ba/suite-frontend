<div class="ui grid">
  <div class="five wide column">
    {{suite-titulo titulo="Acciones"}}
    {{#suite-permiso permiso="agenda.crear"}}
      <button id="crearEvento" class="ui button primario " {{action (route-action "crearUnEventoNuevo")}}><i class="ui plus icon"></i> Nueva Acción</button>
    {{/suite-permiso}}
  </div>
  <div class="eleven wide column">
    {{suite-estadisticas/eventos tarea=model.estadisticas}}
  </div>
</div>


<div class="ui divider"></div>

<div class="ui secondary pointing fluid menu">
    <div class="menu">
      {{#link-to "app.agenda.index.calendario" class="ui item"}}CALENDARIO{{/link-to}}
      {{#link-to "app.agenda.index.lista" class="ui item"}}LISTA{{/link-to}}
    </div>
</div>

{{outlet}}
