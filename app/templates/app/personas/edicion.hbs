{{outlet}}

{{suite-titulo titulo="Edición de usuario"}}

{{suite-formularios/usuario
  model        = (changeset model.usuario model.validaciones)
  contratos    = model.contratos
  localidad    = model.localidad
  on-submit    = (action "guardar")
  submit-label = 'Guardar'
}}
