machine:
  node:
    version: "6.9.2"
dependencies:
  pre:
    - npm install -g bower
    - sudo apt-get update; sudo apt-get install -y --only-upgrade google-chrome-stable
  post:
    - bower install
deployment:
  production:
    branch: master
    commands:
      - git remote add dokku dokku@enjambrelab.com.ar:suite
      - git push dokku master:master
  productionDTE:
    branch: master
    commands:
      - git remote add dokku-dtelab dokku@dtelab.com.ar:suite2
      - git push dokku-dtelab master:master
